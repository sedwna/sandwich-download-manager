{
  "schemaVersion": "1.0.0",
  "planId": "ENGPLAN-CYCLE-EVT-20260803-002",
  "workstreamId": "WS-15",
  "ownerRole": "ENG-15",
  "producerActorId": "actor-eng-15",
  "status": "failed",
  "verificationDisposition": "failed",
  "implementationRevision": "pending",
  "changedComponents": [],
  "commands": [
    "Get-Content -Raw -LiteralPath '.development-os\\runs\\ENGPLAN-CYCLE-EVT-20260803-002-WS-15-input.json'",
    "git status --short; git ls-files; Get-ChildItem -Force | Select-Object Mode,Length,Name",
    "Get-Content -Raw package.json; rg --files apps packages src tests .github | Sort-Object; Test-Path Cargo.toml; Test-Path LICENSE",
    "Get-ChildItem -File -Recurse apps,packages,src,tests,.github | ForEach-Object { $_.FullName.Substring($PWD.Path.Length + 1) } | Sort-Object",
    "Get-Content -Raw tests\\quality\\verify-windows-delivery.ps1; Get-Content -Raw tests\\quality\\run-regression.ps1; Get-Content -Raw apps\\desktop\\Cargo.toml; Get-Content -Raw packages\\download-engine\\Cargo.toml",
    "node --test tests\\frontend\\*.test.js; git diff --check",
    "$env:CARGO_TARGET_DIR = Join-Path ([System.IO.Path]::GetTempPath()) 'sandwich-ws15-cargo-target'; cargo fmt --manifest-path packages\\download-engine\\Cargo.toml --all -- --check",
    "$env:CARGO_TARGET_DIR = Join-Path ([System.IO.Path]::GetTempPath()) 'sandwich-ws15-cargo-target'; cargo test --manifest-path packages\\download-engine\\Cargo.toml --locked",
    "$env:CARGO_TARGET_DIR = Join-Path ([System.IO.Path]::GetTempPath()) 'sandwich-ws15-cargo-target'; cargo check --manifest-path apps\\desktop\\Cargo.toml --locked",
    "$testExe = Get-ChildItem -LiteralPath packages\\download-engine\\target\\debug\\deps -Filter 'runtime-*.exe' | Sort-Object LastWriteTime -Descending | Select-Object -First 1; & $testExe.FullName --nocapture",
    "Test-Path -LiteralPath Cargo.toml; Test-Path -LiteralPath LICENSE; git status --short; Get-Date -Format o"
  ],
  "evidence": [
    "Node frontend tests completed: 2 passed, 0 failed. These cover formatter and canonical-state labels but are not real-DOM behavioral evidence.",
    "Rust engine test suite completed from current source using an external temporary target directory: 6 passed, 0 failed. Covered segmented and fallback byte correctness, persisted pause/resume, cancellation, restart recovery, manifest safety, filename containment, and bounded metrics.",
    "Windows Tauri adapter cargo check completed successfully against the shared download-engine crate.",
    "git diff --check exited successfully, with only LF-to-CRLF warnings for pre-existing working-copy files.",
    "cargo fmt --check failed because packages/download-engine/tests/runtime.rs is not rustfmt-compliant.",
    "Repository-root Cargo.toml is absent, so the required root Rust workspace was not verified.",
    "Repository-root LICENSE is absent, so the required GPL-3.0 license text was not verified.",
    "No real DOM/browser/Tauri runtime test, keyboard-only test, or screen-reader test was available or reproduced for queue behavior, clipboard offers, destination selection, or Windows shell actions.",
    "Final git status retained the same tracked modifications and implementation-related untracked paths observed before verification; this verifier edited no repository files."
  ],
  "knownRisks": [
    "The application sources and most delivery artifacts are untracked, so there is no canonical Git revision against which implementation provenance can be established.",
    "Passing source compilation and engine integration tests do not establish the required user-visible Windows runtime behavior or accessibility.",
    "The missing root workspace manifest and GPL-3.0 LICENSE materially violate AC-01, AC-12, AC-25, AC-29, and the approved product constraints.",
    "Formatting verification failure prevents the documented verification suite from passing."
  ],
  "completedAt": "2026-08-03T20:09:37.6475388+03:30"
}
