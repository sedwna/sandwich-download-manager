{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main window. The UI only needs to listen for engine events; every privileged operation (file dialogs, clipboard, opening files) happens in Rust behind a command, so no plugin permissions are granted to the webview.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
